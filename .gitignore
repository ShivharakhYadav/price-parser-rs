# Rust
/target/
**/*.rs.bk
Cargo.lock.bak

# Python
.venv/
__pycache__/
*.py[cod]
*.so
*.pyd
.pytest_cache/
*.egg-info/
build/
dist/

# Fuzzer tables. Removed automatically on success and kept only when a run
# fails, so they can be replayed -- never committed either way.
fuzz-*.tsv

# Editors / OS
.vscode/
.idea/
.DS_Store
Thumbs.db
